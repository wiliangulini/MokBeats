// Registrado como "setupFiles" adicional (junto de src/test.ts) do alvo
// test-focus (@angular/build:unit-test, runner: vitest). A descoberta dos
// specs "focados" é feita via tsconfig.spec.focus.json's "include" — este
// arquivo existe só para preservar a distinção test vs test:focus.
